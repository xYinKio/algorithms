package numbers.findZeroes

/***
 * TODO find zeroes by Newton-Raphson
 *
 * Нужны: функция, её производная, начальное приближение для корна, допустимая погрешность
 * 1. Пускаем касательную до пересечения с абциссой
 * 2. Ведём вертикальную линию вдоль ординат до пересечения  с функцией
 * 3. Повторяем с первого пункта, пока не найдём 0
 *
 * В случае с несколькими Запустить функцию с другим приближённым корнем.
 */